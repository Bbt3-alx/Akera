paths:
  "/api/v1/partners/new":
    post:
      summary: Legacy partner route disabled
      description: Legacy partner CRUD is disabled. Use company memberships and invitations instead.
      tags: [Partners]
      security:
        - bearerAuth: []
      responses:
        "410":
          description: Legacy partner route disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  code:
                    type: integer
                    example: 410
                  message:
                    type: string
                    example: Legacy partner route disabled. Use company memberships instead.

  "/api/v1/partners":
    get:
      summary: Legacy partner route disabled
      description: Legacy partner listing is disabled. Use company memberships and invitations instead.
      tags: [Partners]
      security:
        - bearerAuth: []
      responses:
        "410":
          description: Legacy partner route disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  code:
                    type: integer
                    example: 410
                  message:
                    type: string
                    example: Legacy partner route disabled. Use company memberships instead.

  "/api/v1/partners/{id}":
    get:
      summary: Legacy partner route disabled
      description: Legacy partner detail reads are disabled. Use company memberships and invitations instead.
      tags: [Partners]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdPath"
      responses:
        "410":
          description: Legacy partner route disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  code:
                    type: integer
                    example: 410
                  message:
                    type: string
                    example: Legacy partner route disabled. Use company memberships instead.

  "/api/v1/partners/{id}/update":
    put:
      summary: Legacy partner route disabled
      description: Legacy partner updates are disabled. Use company memberships and invitations instead.
      tags: [Partners]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdPath"
      responses:
        "410":
          description: Legacy partner route disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  code:
                    type: integer
                    example: 410
                  message:
                    type: string
                    example: Legacy partner route disabled. Use company memberships instead.

  "/api/v1/partners/{id}/balance":
    get:
      summary: Legacy partner route disabled
      description: Legacy partner balance reads are disabled. Use membership-context company data instead.
      tags: [Partners]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdPath"
      responses:
        "410":
          description: Legacy partner route disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  code:
                    type: integer
                    example: 410
                  message:
                    type: string
                    example: Legacy partner route disabled. Use company memberships instead.

  "/api/v1/partners/{id}/remove":
    put:
      summary: Legacy partner route disabled
      description: Legacy partner removal is disabled. Use company memberships and invitations instead.
      tags: [Partners]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdPath"
      responses:
        "410":
          description: Legacy partner route disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  code:
                    type: integer
                    example: 410
                  message:
                    type: string
                    example: Legacy partner route disabled. Use company memberships instead.

  "/api/v1/partners/{id}/restore":
    put:
      summary: Legacy partner route disabled
      description: Legacy partner restore is disabled. Use company memberships and invitations instead.
      tags: [Partners]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdPath"
      responses:
        "410":
          description: Legacy partner route disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  code:
                    type: integer
                    example: 410
                  message:
                    type: string
                    example: Legacy partner route disabled. Use company memberships instead.
